#include <iostream>
#include <random>
#include <string> 

using namespace std;


//! 
    /*! It generates the IP this is returned as a string it also calls rand for each section for the IP. Also done using a for 
    loop.
     
    \return a string of IP addr
    */

string generateIP () ;